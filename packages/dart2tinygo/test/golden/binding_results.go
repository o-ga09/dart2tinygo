// Code generated by dart2tinygo. DO NOT EDIT.
package main

import (
	rt "example.com/fake/runtime"
	"strconv"
)

func main() {
	widget := rt.NewWidget()
	level := rt.ReadLevel()
	ready := rt.IsReady()
	volts := rt.Voltage()
	name := rt.Label()
	color := rt.RGB(255, 0, 0)
	button := rt.ButtonA
	gain := 2.0
	on := false
	title := "main"
	widget.Fill(color)
	widget.Fill(rt.Red)
	widget.Fill(rt.RGB(0, 255, 0))
	widget.Configure(ready, gain, name)
	widget.Configure(on, volts, title)
	widget.Configure(true, 0.5, "x")
	pressed := widget.Press(button)
	other := widget.Press(rt.ButtonB)
	x := widget.Level()
	println(name)
	println(rt.Label())
	println("level " + strconv.Itoa(level) + " ready " + strconv.FormatBool(ready) + " name " + name)
	println("x " + strconv.Itoa(x) + " other " + strconv.FormatBool(other) + " pressed " + strconv.FormatBool(pressed))
	println("live " + strconv.Itoa(widget.Level()) + " " + strconv.FormatBool(rt.IsReady()) + " " + rt.Label())
}
