name: hello_wioterminal
description: Shows "Hello From Dart !" on the Wio Terminal's LCD via dart2tinygo.
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  # Resolved to ../../packages/wio_terminal by the workspace. Outside this
  # repository, depend on the published package instead.
  wio_terminal: ^0.0.2