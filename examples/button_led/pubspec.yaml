name: button_led
description: Lights the LED while button A is held, via dart2tinygo on the Wio Terminal.
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  # Resolved to ../../packages/wio_terminal by the workspace. Outside this
  # repository, depend on the published package instead.
  wio_terminal: ^0.0.1
